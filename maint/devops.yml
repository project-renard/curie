native:
  debian:
    packages:
      - pkg-config
      - libexpat1-dev
  macos-homebrew:
    packages: ~
  msys2-mingw64:
    packages:
      - mingw-w64-x86_64-expat
      - mingw-w64-x86_64-openssl
